#include <stdio.h>

struct Point {
    int x;
    int y;
    int a;
    int c;
    int b;
};

int main() {
    float result = 15.10;
    int a = 5;
    int b = 3;

    // Asignación y llamada a función (StatementRest)
    point1.x = 10;
    point1.y = 20;
    while (result > 0) {
        add(5,10)
    }

    return 0;
}
